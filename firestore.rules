rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Helper functions
    function isAuthenticated() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isAuthenticated() && request.auth.uid == userId;
    }

    function isAgent() {
      return isAuthenticated() &&
             exists(/databases/$(database)/documents/agents/$(request.auth.uid));
    }

    // Users collection
    match /users/{userId} {
      allow read: if isAuthenticated();
      allow write: if isOwner(userId);
    }

    // Customers collection
    match /customers/{customerId} {
      allow read: if isAuthenticated();
      allow write: if isOwner(customerId);
    }

    // Agents collection
    match /agents/{agentId} {
      allow read: if true; // Public read for customer tracking
      allow write: if isOwner(agentId);
    }

    // Orders collection - RELAXED FOR DEVELOPMENT/TESTING
    match /orders/{orderId} {
      // Allow all authenticated users to read and write orders
      // In production, you should restrict this further
      allow read, write: if isAuthenticated();
    }

    // Chats collection
    match /chats/{chatId} {
      allow read, write: if isAuthenticated();

      match /messages/{messageId} {
        allow read, create: if isAuthenticated();
      }
    }

    // Earnings (agent only)
    match /earnings/{earningId} {
      allow read, write: if isAuthenticated();
    }

    // Payout requests
    match /payoutRequests/{payoutId} {
      allow read, write: if isAuthenticated();
    }

    // Notifications
    match /notifications/{notifId} {
      allow read: if isAuthenticated();
      allow create: if isAuthenticated();
    }
  }
}
